# ML course exercises (weeks 0–10)
numpy>=1.24
pandas>=2.1
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.8

# Deep-learning weeks (11, 13–15) — CPU is enough
torch>=2.1.0

# Optional: preview the site
mkdocs-material>=9.5

# Optional: LangChain / LangGraph snippets
langchain>=0.2
langgraph>=0.2
python-dotenv>=1.0
requests>=2.31

# Warehouse-shaped SQL on the CSVs (week 16)
duckdb>=1.0

# Dev
pytest>=7.4
