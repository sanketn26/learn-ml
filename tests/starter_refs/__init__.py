"""Reference answers for exercises/ml/*/starter.py, used only to prove the checks.

Each module mirrors one starter's task_N functions. If a check rejects the
reference, the check is wrong (or the data changed) — not the learner.
"""
