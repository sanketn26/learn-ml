# Reference environment for the API generation shown in the lessons (LangChain / LangGraph 1.x).
# Use a dedicated virtual environment. See docs/framework-tracks.md.
# Python 3.11+ (3.12 tested).
langchain==1.4.2
langchain-core==1.6.4
langchain-text-splitters==1.1.2
langgraph==1.2.12
# Durable checkpoints for the agent capstone (a paused refund survives a restart).
langgraph-checkpoint-sqlite==3.1.1
python-dotenv==1.0.1
requests==2.32.3
pydantic==2.13.5
# Week 6 serves the chain behind FastAPI (TestClient uses httpx).
fastapi==0.141.1
httpx
# Week 4 measures retrieval with a TF-IDF baseline.
scikit-learn
scipy
# langchain-community is being sunset; the lessons use langchain_core's fake models instead.
