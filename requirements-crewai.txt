# CrewAI is intentionally isolated from the LangChain/LangGraph environment.
# This pin is the CrewAI 1.x API generation used in the lessons.
# Python 3.11+ (3.12 tested; CrewAI requires < 3.14).
crewai==1.15.22
# crewai 1.15 requires python-dotenv>=1.2.2; do not pin it below that.
python-dotenv>=1.2.2
pytest
